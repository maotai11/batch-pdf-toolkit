# 📘 使用指南

## 快速開始

### 1. 開啟應用程式

直接用 Chrome 瀏覽器開啟 `index.html` 檔案即可使用。

### 2. 載入 PDF 檔案

**方式一：拖曳上傳**
- 將 PDF 檔案拖曳到左側「檔案列表」區域
- 支援一次上傳多個檔案

**方式二：點擊選擇**
- 點擊左側上傳區域
- 從檔案選擇器選擇 PDF 檔案

### 3. 編輯 PDF

#### 單頁編輯功能

**旋轉頁面**
- 點擊 `↶ 左轉` 或 `↷ 右轉` 按鈕
- 每次旋轉 90 度

**裁切頁面**
1. 點擊 `✂ 裁切` 按鈕啟用裁切模式
2. 拖曳調整裁切框的位置和大小
3. 再次點擊 `✂ 裁切` 應用裁切

**添加註解**
- `T 文字`：添加可編輯文字
- `□ 矩形`：添加矩形框
- `○ 圓形`：添加圓形標記

**刪除物件**
- 選取註解物件
- 點擊 `🗑 刪除` 移除

#### 頁面導航

**切換頁面**
- 點擊底部縮圖列表中的任一頁面
- 當前頁面會有藍色邊框標示

**查看頁面資訊**
- 右側面板顯示當前頁碼、總頁數、檔案大小

### 4. 設定匯出選項

#### 壓縮品質

**調整 JPEG 品質**（右側面板 > 匯出設定）
- 拖曳滑桿調整品質（10-100%）
- **建議值：85%**（平衡品質與檔案大小）
- 90%+：最高品質，檔案較大
- 60-80%：中等品質，檔案適中
- <60%：較小檔案，品質下降

#### 命名規則

**設定檔案名稱格式**（右側面板 > 命名規則）

**可用變數：**
- `{原始檔名}`：原 PDF 檔案名稱
- `{頁碼}`：當前頁碼
- `{流水號}`：4 位數流水號（0001, 0002...）
- `{日期}`：當前日期（YYYY-MM-DD）
- `{時間}`：當前時間（HH-MM-SS）

**範例：**
```
{原始檔名}-{頁碼}
→ 報告-1.pdf, 報告-2.pdf, 報告-3.pdf

{日期}-{原始檔名}-p{頁碼}
→ 2026-02-02-報告-p1.pdf

掃描_{流水號}
→ 掃描_0001.pdf, 掃描_0002.pdf
```

**快速插入變數：**
- 點擊變數按鈕自動插入到命名規則輸入框
- 預覽區域會即時顯示範例檔名

### 5. 匯出 PDF

**匯出當前頁面**
1. 編輯完成後點擊右上角 `匯出 PDF` 按鈕
2. 瀏覽器會自動下載處理後的 PDF

**批量操作**（右側面板 > 批量操作）

- **旋轉全部頁面**：對所有頁面套用相同旋轉
- **合併全部 PDF**：將多個 PDF 合併為一個
- **拆分為單頁**：將每一頁輸出為獨立 PDF
- **擷取特定頁面**：選擇特定頁碼進行匯出

---

## 進階技巧

### 記憶體管理

**處理大量檔案時的建議：**
1. 避免一次載入超過 50 個 PDF
2. 處理完一批後點擊「清空全部」釋放記憶體
3. 關閉其他瀏覽器分頁以獲得更多記憶體

### 效能優化

**加快處理速度：**
- 降低壓縮品質（60-70%）可顯著提升速度
- 批量處理時建議分批進行
- 避免在低規格裝置上處理超過 20 頁的 PDF

### 品質建議

**根據用途選擇品質：**
- **存檔備份**：90-100%（高品質保存）
- **日常使用**：80-85%（建議值）
- **電子郵件附件**：60-70%（減少檔案大小）
- **網頁預覽**：50-60%（快速載入）

---

## 快捷鍵

目前版本暫不支援快捷鍵，計劃在未來版本加入：
- `Ctrl/Cmd + Z`：復原
- `Ctrl/Cmd + Y`：重做
- `Delete`：刪除選取物件
- `Ctrl/Cmd + A`：全選
- `方向鍵`：微調物件位置

---

## 常見問題

### Q1: 為什麼無法載入 PDF？

**可能原因：**
- PDF 檔案損壞或加密
- 檔案過大（建議單檔 < 50MB）
- 瀏覽器不支援（請使用 Chrome 最新版）

**解決方法：**
1. 確認 PDF 可正常開啟
2. 嘗試使用其他 PDF 編輯軟體解除密碼保護
3. 將大檔案拆分為多個小檔案

### Q2: 匯出的 PDF 檔案很大？

**解決方法：**
1. 降低壓縮品質（調整滑桿到 60-70%）
2. 減少註解和圖形物件
3. 避免使用高解析度的背景圖片

### Q3: 編輯後的內容消失了？

**注意事項：**
- 切換頁面或檔案前，先點擊「匯出 PDF」儲存
- 註解只存在於當前編輯階段
- 關閉瀏覽器前務必匯出所需檔案

### Q4: 批量處理失敗？

**檢查清單：**
- 確認已載入所有需要的 PDF 檔案
- 檢查瀏覽器控制台是否有錯誤訊息
- 嘗試減少一次處理的檔案數量
- 清空瀏覽器快取後重試

### Q5: 在行動裝置上能用嗎？

**限制說明：**
- 本工具主要針對桌面版 Chrome 優化
- 行動裝置可能因效能限制無法順暢運作
- 建議使用桌面電腦或筆記型電腦

---

## 瀏覽器相容性

✅ **完全支援：**
- Chrome 90+（推薦）
- Edge 90+

⚠️ **部分支援：**
- Firefox（可能有顯示問題）
- Safari（功能受限）

❌ **不支援：**
- IE 11 及更舊版本

---

## 隱私與安全

🔒 **完全離線運作**
- 所有處理都在您的瀏覽器中進行
- 檔案不會上傳到任何伺服器
- 無需網路連線即可使用

📁 **資料處理**
- 關閉瀏覽器後，所有資料自動清除
- 不會在裝置上留下任何快取檔案
- 匯出的 PDF 儲存在您指定的下載位置

---

## 技術支援

遇到問題？

1. 查看 [常見問題](#常見問題) 章節
2. 檢查瀏覽器控制台錯誤訊息（F12 開啟）
3. 到 GitHub Issues 回報問題
4. 附上錯誤截圖和操作步驟以便快速解決

---

**版本：** 1.0.0  
**最後更新：** 2026-02-02
